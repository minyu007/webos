'use strict';
define(['app'], function(app) {
	app.register.controller('registerCtrl', ['$scope', '$http', '$location', 'userService',
		function($scope, $http, $location, userService) {

		}
	]);
});